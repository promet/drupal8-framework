drupal:
    settings:
        db_url: "mysql://default:default@localhost/default"
        databases:
            default:
                default:
                    database: default
                    username: default
                    password: default
                    driver: mysql
                    host: localhost
